// external import
import { Router } from "express";

// create setting router
const router = Router();

// define setting route

// change password
router.post("/chnage-password", () => {});

// forget password
router.post("/forgot-password", () => {});

// export
export default router;
